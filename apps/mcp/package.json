{
  "name": "@awesome-bookmarks/mcp",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Model Context Protocol server for AwesomeBookmarks — lets an AI add, search and manage bookmarks.",
  "bin": {
    "awesomebookmarks-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint'",
    "test": "echo 'no tests'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
